# PROJECT_RULES

이 문서는 이 프로젝트에만 적용되는 rules를 기록한다.

## 프로젝트 목표

- Windows에서 동작하는 `vDrop` 배치 다운로드 앱을 만든다.
- 사용자는 여러 링크를 붙여넣고 비디오/오디오 모드를 고른 뒤 `yt-dlp`로 일괄 다운로드할 수 있어야 한다.
- 입력창은 구조화 편집기처럼 동작해야 하며, URL, 파일명 후보, 폴더 선언을 자동 해석해야 한다.
- UI는 Fluent 기반의 정돈된 Windows 앱처럼 보여야 한다.

## 우선 적용 규칙

- 이 파일의 규칙은 `shared-rules`의 공통 규칙보다 우선한다.
- 공통 규칙과 충돌하면 이 문서를 따르고, 모호하면 사용자에게 확인한다.
- 여러 프로젝트에 반복 적용될 규칙은 이 파일이 아니라 `../../shared-rules/`에 추가한다.

## 기술 스택

- Electron
- React
- TypeScript
- Vite
- Windows 네이티브 스타일의 CSS 시스템
- Node.js `child_process` 기반 `yt-dlp` 실행

## 실행 규칙

- 개발 서버는 `npm run dev`를 사용한다.
- 브라우저 확인은 `npm run dev:web`로 띄운 Vite 화면을 우선 검증한다.
- 앱 실행은 `npm start`를 사용한다.
- 빌드는 `npm run build`를 사용한다.
- 기본 개발 포트는 `4173`이다.
- Vite 미리보기 포트는 `4174`이다.
- 다운로드 엔진은 환경변수보다 로컬 `PATH`와 일반 설치 경로를 우선 탐색한다.
- `yt-dlp`와 `ffmpeg`가 없으면 앱은 경고만 표시하고 중단하지 않는다.
- 다운로드 기본 저장 위치는 `~/Downloads/vDrop`다.

## 작업 원칙

- 요청 범위를 넘는 대규모 리팩터링은 하지 않는다.
- 변경 전 영향 범위를 짧게 요약한다.
- 보안 정보, API 키, `.env`, 개인정보를 커밋하지 않는다.

## 검증 방법

- `npm run build`
- `npm run dev:web`
- 브라우저에서 첫 화면 확인

